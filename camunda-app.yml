eureka:
  instance:
    appname: camunda-app
  client:
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

camunda:
  bpm:
    admin-user:
      id: user
      password: userSecret
      firstName: User
    filter:
      create: All tasks

org:
  camunda:
    optimize: debug
